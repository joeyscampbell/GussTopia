import React from 'react';
import './Hero.css';

const Hero = () => (
  <section className="hero">
    <h1>Welcome to the Neon Lounge</h1>
    <button aria-label="Explore">Enter</button>
  </section>
);

export default Hero;
